{
  "name": "Rust + Windows (GNU) cross-compile",
  "image": "mcr.microsoft.com/devcontainers/rust:1-bookworm",
  "remoteUser": "vscode",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mingw-w64 nodejs npm && rustup target add x86_64-pc-windows-gnu && sudo npm install -g @openai/codex && codex --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer"
      ]
    }
  }
}
