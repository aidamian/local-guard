{
  "name": "Rust + Windows (GNU) cross-compile",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "postCreateCommand": "rustup target add x86_64-pc-windows-gnu && sudo npm install -g @openai/codex && codex --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer"
      ]
    }
  }
}
